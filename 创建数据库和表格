SHOW DATABASES;                    *显示所有的database

CREATE DATABASE hello_world_db;	   *创建新的database

DROP DATABASE hello_world_db;      *删除已有的database, 一经drop，无法恢复

USE DATABASE ();                   *使用database

SELECT DATABASE ();                *确定当前所在database；
                                   *如果数据库不包含此database，则会提示“null”

 *  “；”不要忘记； 为便于区分，建议code用大写，数据库信息用小写

 *数据库就是一群表格的集合，但不是所有的数据库都是
 *columns = headers
 *rows    = the actual data
	
 *数字：int （< 4294967295）
 *文字：varchar ( a variable-length string) vs char ( fixed length , eg:10)
	   1< varchar <255
           eg: 'coffee!!' & '-000'

  CREATE TABLE cats                 *创建表格：创建database → 使用database → 创建表格 → 创建表格的行
  (
  name VARCHAR(100),
  age INT
  );
  SHOW TABLES;
  SHOW COLUMNS FROM cats; 或者 DESC cats;
